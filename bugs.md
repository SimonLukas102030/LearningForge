Hier sind verschiedende Bugs, die bemerkt wurden.

## AI Features probleme
Bei den test, werden fragen, wo man in sätzen antworten muss, nicht korrigiert und man bekommt keine punkte. Außerdem, werden keine Fragen custom generiert. Es scheint so, als würde gemini garnicht funktionieren.

## Colorpicker Probleme
Das ressetten von Farben setzt es nicht auf den Standert zurück. Es sollte es zurücksetzten, zu dem, was in der subject-configs.json steht. 
