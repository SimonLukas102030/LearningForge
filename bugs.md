Hier sind verschiedende Bugs, die bemerkt wurden.

1. Bei den test, werden fragen, wo man in sätzen antworten muss, nicht korrigiert und man bekommt keine punkte. Außerdem, werden keine Fragen custom generiert. Es scheint so, als würde gemini garnicht funktionieren.
